import '../../../core/network/api_client.dart'; import '../domain/mode.dart'; class ModeRepository { ModeRepository(this._client); final ApiClient _client; Future<List<Mode>> getModes() async { final data=await _client.getModes(); final items=data['modes']; if(items is! List)return const []; return items.whereType<Map>().map((e)=>Mode.fromJson(Map<String,dynamic>.from(e))).toList(growable:false); } }