import '../../../core/network/api_client.dart'; import '../domain/category.dart'; class CategoryRepository { CategoryRepository(this._client); final ApiClient _client; Future<List<Category>> getCategories() async { final data=await _client.getCategories(); final items=data['categories']; if(items is! List)return const []; return items.whereType<Map>().map((e)=>Category.fromJson(Map<String,dynamic>.from(e))).toList(growable:false); } }